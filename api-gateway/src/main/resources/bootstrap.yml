management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  endpoint:
    health:
      show-details: always
    gateway:
      enabled: true
  metrics:
    export.prometheus.enabled: true